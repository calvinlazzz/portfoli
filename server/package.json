{
  "name": "portfolio-app-server",
  "version": "1.0.0",
  "description": "Server-side application for the portfolio app",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "knex": "knex",
    "migrate:make": "knex migrate:make -x js --cwd .",
    "migrate:latest": "knex migrate:latest --cwd .",
    "seed:run": "knex seed:run --cwd .",
    "lint": "eslint ./src",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "knex": "^3.1.0",
    "pg": "^8.16.3",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "engines": {
    "node": ">=14.x"
  },
  "license": "MIT"
}
